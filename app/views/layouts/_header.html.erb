
<header>
  <div class="header-container">
    <h1>
      <%= link_to "Reservar APP", root_path %>
    </h1>
    <% if logged_in? %>
      <input type="checkbox" id="check">
      <label class="drop-button" for="check">
        <div class="logged-in-nav-list">
          <% if current_user.avatar_icon.attached? %>
            <%= image_tag current_user.avatar_icon, size: '50x50', alt: "アイコン画像" %>
          <% else %>
            <%= image_tag 'default-avatar.png', size: '50x50', alt: "アイコン画像" %>
          <% end %>
          <%= current_user.name %>
        </div>
      </label>
      <nav class="gloval-nav">
        <ul>
          <li class="add-room">施設の新規登録</li>
          <li>予約済み一覧</li>
          <li>登録済み一覧</li>
          <%= link_to users_account_path do %>
            <li>アカウント設定</li>
          <% end %>
          <%= link_to log_out_path, method: :delete, class: "logout_button" do %>
            <li class="logout">ログアウト</li>
          <% end %>
        </ul>
      </nav>
    <% else %>
      <div class="nav-list">
        <ul>
          <li>
            <%= link_to "ログイン", log_in_path, class: "login_button" %>
          </li>
          <li>
            <%= link_to "新規登録", sign_up_path, class: "sign_up_button" %>
          </li>
        </ul>
      </div>
    <% end %>
  </div>
</header>
